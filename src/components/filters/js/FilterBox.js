import React from 'react';

import '../css/FilterBox.css';

const FilterBtn = ({ filterBox }) => {



    if(filterBox) {
        return (
            <div id="filter-box">
                
            </div>
        )
    }
};

export default FilterBtn;